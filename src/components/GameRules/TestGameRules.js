import React from 'react';
import {shallow, mount, render} from 'enzyme';
import GameRules from "./GameRules";

describe("GameRules", () => {
    it("can render the GameRules component", () => {

    });
});