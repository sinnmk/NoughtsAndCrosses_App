import React from 'react';
import {shallow, mount, render} from 'enzyme';
import LandingPage from "./LandingPage";

describe("LandingPage", () => {
    it("can render the Landing Page component", () => {

    });
});